# 🚀 Пошаговая инструкция: Настройка Email-верификации

> **Время на настройку:** ~10 минут  
> **Сложность:** Легко ⭐

---

## 📋 Что мы будем делать

1. ✅ Отключим автоматическое подтверждение email при регистрации
2. ✅ Загрузим красивые email-шаблоны в Supabase
3. ✅ Настроим отправку писем (встроенный сервис Supabase)
4. ✅ Протестируем регистрацию с подтверждением

---

## Шаг 1️⃣: Изменяем код на сервере

### 1.1. Откройте файл сервера

Найдите файл: `/supabase/functions/server/index.tsx`

### 1.2. Найдите endpoint `/signup`

Прокрутите до строки ~42, где находится:

```typescript
app.post('/make-server-05bdbe69/signup', async (c) => {
```

### 1.3. Удалите строку `email_confirm: true`

**❌ Было (строки 62-71):**
```typescript
const { data, error } = await supabase.auth.admin.createUser({
  email,
  password,
  user_metadata: { 
    name: name || email.split('@')[0],
    role: userRole,
  },
  // Автоматически подтверждаем email, так как сервер email не настроен
  email_confirm: true, // ← УДАЛИТЕ эту строку!
});
```

**✅ Стало:**
```typescript
const { data, error } = await supabase.auth.admin.createUser({
  email,
  password,
  user_metadata: { 
    name: name || email.split('@')[0],
    role: userRole,
  },
  // Email будет подтверждаться через письмо
});
```

### 1.4. Сохраните файл

Нажмите `Ctrl+S` (или `Cmd+S` на Mac)

---

## Шаг 2️⃣: Загружаем шаблоны в Supabase

### 2.1. Откройте Supabase Dashboard

1. Перейдите на [app.supabase.com](https://app.supabase.com)
2. Выберите ваш проект
3. В левом меню нажмите **Authentication**
4. Перейдите во вкладку **Email Templates**

### 2.2. Загрузите шаблон "Confirm signup"

#### Шаг A: Откройте файл шаблона
В VS Code откройте: `/email-templates/confirm-signup.html`

#### Шаг Б: Скопируйте весь код
```
Ctrl+A  (выделить всё)
Ctrl+C  (скопировать)
```

#### Шаг В: Вставьте в Supabase
1. В Supabase нажмите **Confirm signup**
2. Удалите весь старый код из поля "Message Body"
3. Вставьте скопированный код: `Ctrl+V`
4. Нажмите **Save** (в правом нижнем углу)

#### Шаг Г: Настройте Subject (тему письма)

В поле **Subject** введите:
```
Подтверждение email — Habit Tracker
```

Или на английском:
```
Confirm Your Email — Habit Tracker
```

### 2.3. Загрузите шаблон "Reset password"

Повторите те же шаги для файла: `/email-templates/reset-password.html`

**Subject:**
```
Сброс пароля — Habit Tracker
```

### 2.4. Загрузите шаблон "Change email address"

Повторите для файла: `/email-templates/change-email.html`

**Subject:**
```
Подтверждение нового email — Habit Tracker
```

### 2.5. (Опционально) Загрузите шаблон "Magic Link"

Если планируете использовать вход без пароля:

Файл: `/email-templates/magic-link.html`

**Subject:**
```
Ваша ссылка для входа — Habit Tracker
```

---

## Шаг 3️⃣: Настраиваем Email Auth

### 3.1. Включите Email Confirmations

1. В Supabase Dashboard откройте **Authentication** → **Settings**
2. Прокрутите до раздела **Email Auth**
3. Убедитесь, что включено:
   - ✅ **Enable Email Signup**
   - ✅ **Enable Email Confirmations**

### 3.2. Настройте Redirect URLs

1. Прокрутите до раздела **URL Configuration**
2. В поле **Site URL** введите URL вашего приложения:
   ```
   http://localhost:5173
   ```
   _(для продакшена замените на ваш домен)_

3. В поле **Redirect URLs** добавьте:
   ```
   http://localhost:5173/**
   https://your-production-domain.com/**
   ```

### 3.3. (Опционально) Настройте SMTP

**Если хотите использовать встроенный email Supabase** (рекомендуется для старта):
- Ничего не делайте, всё уже работает! 🎉
- Бесплатно до 30,000 писем/месяц

**Если хотите использовать свой SMTP:**

1. Прокрутите до **SMTP Settings**
2. Включите **Enable Custom SMTP**
3. Введите данные:
   ```
   Host:     smtp.gmail.com          (пример для Gmail)
   Port:     587
   Username: your-email@gmail.com
   Password: your-app-password
   Sender:   Habit Tracker <noreply@yourapp.com>
   ```

**Популярные провайдеры:**
- **SendGrid:** 100 писем/день бесплатно
- **Mailgun:** 5,000 писем/месяц бесплатно
- **Amazon SES:** ~$0.10 за 1,000 писем
- **Resend:** 3,000 писем/месяц бесплатно

### 3.4. Сохраните изменения

Нажмите **Save** внизу страницы

---

## Шаг 4️⃣: Тестируем регистрацию

### 4.1. Очистите старых тестовых пользователей (опционально)

1. **Authentication** → **Users**
2. Удалите всех тестовых пользователей (чтобы начать с чистого листа)

### 4.2. Создайте нового пользователя

1. Откройте ваше приложение: `http://localhost:5173`
2. Перейдите на страницу регистрации
3. Введите данные:
   ```
   Email: your-real-email@gmail.com  ← используйте реальный email!
   Password: TestPassword123
   Name: Test User
   ```
4. Нажмите **Зарегистрироваться**

### 4.3. Проверьте почту

1. Откройте ваш email-клиент
2. **Проверьте папку "Спам"** (первые письма часто попадают туда)
3. Вы должны увидеть красивое письмо с темой:
   ```
   Подтверждение email — Habit Tracker
   ```

### 4.4. Подтвердите email

1. Откройте письмо
2. Нажмите кнопку **"Подтвердить Email"**
3. Вас должно перенаправить в приложение

### 4.5. Проверьте статус пользователя

1. Вернитесь в Supabase Dashboard
2. Откройте **Authentication** → **Users**
3. Найдите вашего пользователя
4. В колонке **Email Confirmed** должно быть:
   - ✅ **Yes** (зеленая галочка)

---

## ✅ Проверочный чеклист

Убедитесь, что всё работает:

- [ ] Удалил `email_confirm: true` из `/supabase/functions/server/index.tsx`
- [ ] Сохранил файл сервера
- [ ] Загрузил шаблон **Confirm signup** в Supabase
- [ ] Загрузил шаблон **Reset password** в Supabase
- [ ] Загрузил шаблон **Change email** в Supabase
- [ ] Включил **Enable Email Confirmations** в настройках Auth
- [ ] Настроил **Site URL** и **Redirect URLs**
- [ ] Создал тестового пользователя
- [ ] Получил письмо на почту (проверил "Спам")
- [ ] Подтвердил email по ссылке
- [ ] Пользователь помечен как "Email Confirmed: Yes"

---

## 🐛 Что делать, если что-то не работает

### Проблема 1: Письмо не приходит

**Решения:**

1. **Проверьте папку "Спам"**
   - Gmail часто помещает туда первые письма от новых отправителей

2. **Проверьте логи Supabase:**
   - Dashboard → **Logs** → **Edge Functions**
   - Ищите ошибки, связанные с email

3. **Проверьте лимиты:**
   - Free Tier Supabase: 30,000 писем/месяц
   - Если превысили — письма не отправляются

4. **Проверьте SMTP настройки:**
   - Если используете кастомный SMTP, проверьте логин/пароль
   - Попробуйте переключиться на встроенный Supabase Email

### Проблема 2: Письмо пришло, но ссылка не работает

**Решения:**

1. **Проверьте Redirect URLs:**
   - Dashboard → **Authentication** → **Settings** → **URL Configuration**
   - Добавьте `http://localhost:5173/**` для разработки
   - Добавьте `https://your-domain.com/**` для продакшена

2. **Проверьте Site URL:**
   - Должен быть корректный домен (без лишних слешей)

3. **Очистите куки браузера:**
   - Старые сессии могут мешать

### Проблема 3: Пользователь создан, но не может войти

**Решения:**

1. **Проверьте статус email:**
   - Dashboard → **Authentication** → **Users**
   - Колонка **Email Confirmed** должна быть **Yes**

2. **Если Email Confirmed = No:**
   - Пользователь не подтвердил email
   - Отправьте новое письмо подтверждения вручную:
     ```typescript
     const { error } = await supabase.auth.resend({
       type: 'signup',
       email: 'user@example.com'
     });
     ```

3. **Или подтвердите вручную через Dashboard:**
   - Найдите пользователя → **...** → **Confirm email**

### Проблема 4: Дизайн письма сломан

**Решения:**

1. **Проверьте, что скопировали весь HTML:**
   - Должно быть `<!DOCTYPE html>` вверху
   - Должно быть `</html>` внизу

2. **Не редактируйте шаблон в Supabase Dashboard:**
   - Редактируйте только в VS Code
   - Потом копируйте заново в Supabase

3. **Тестируйте в разных email-клиентах:**
   - Gmail
   - Outlook
   - Apple Mail
   - Яндекс.Почта

---

## 📊 Как работает процесс подтверждения

```
┌─────────────────────────────────────────────────────────┐
│ 1. Пользователь заполняет форму регистрации            │
│    Email: user@example.com                             │
│    Password: ••••••••                                  │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 2. Frontend отправляет запрос на сервер                │
│    POST /make-server-05bdbe69/signup                   │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 3. Сервер создаёт пользователя в Supabase Auth        │
│    email_confirm: false ← письмо НЕ подтверждено       │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 4. Supabase автоматически отправляет email             │
│    Шаблон: "Confirm signup"                            │
│    Получатель: user@example.com                        │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 5. Пользователь получает письмо                        │
│    ✉️ "Подтверждение email — Habit Tracker"            │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 6. Пользователь кликает "Подтвердить Email"            │
│    Открывается: https://yourapp.com/confirm?token=...  │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 7. Supabase подтверждает email                         │
│    email_confirm: true ← письмо подтверждено! ✅       │
└─────────────────────────────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│ 8. Пользователь может войти в приложение              │
│    🎉 Готово!                                          │
└─────────────────────────────────────────────────────────┘
```

---

## 🎨 Кастомизация шаблонов

### Как изменить название приложения

Найдите в шаблоне:
```html
<p style="margin: 8px 0 0 0; color: #9ca3af;">
  © {{ formatDate now "2006" }} Habit Tracker. Все права защищены.
</p>
```

Замените `Habit Tracker` на название вашего приложения.

### Как добавить логотип

Добавьте в начале письма (после тега `<td>` заголовка):
```html
<img src="https://your-cdn.com/logo.png" 
     alt="Your Logo" 
     style="max-width: 180px; height: auto; margin-bottom: 24px;">
```

### Как изменить цвет кнопки

Найдите:
```html
<a href="{{ .ConfirmationURL }}" 
   style="background-color: #6366F1; ...">
```

Замените `#6366F1` на нужный цвет (например, `#10b981` для зеленого).

---

## 📞 Нужна помощь?

Если что-то не получается:

1. **Проверьте логи:**
   - Supabase Dashboard → **Logs** → **Edge Functions**
   - Console в браузере (F12)

2. **Читайте документацию:**
   - [Supabase Auth Docs](https://supabase.com/docs/guides/auth)
   - [Email Templates Guide](https://supabase.com/docs/guides/auth/auth-email-templates)

3. **Задайте вопрос:**
   - [Supabase Discord](https://discord.supabase.com)
   - [GitHub Discussions](https://github.com/supabase/supabase/discussions)

---

**Готово! 🎉 Теперь у вас красивые email-уведомления с подтверждением регистрации!**
