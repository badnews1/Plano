# 📧 Email-шаблоны для Supabase

Эта папка содержит HTML-шаблоны для email-уведомлений, которые отправляет Supabase при регистрации, сбросе пароля и других операциях.

## 📁 Содержимое

### 1. `confirm-signup.html` — Подтверждение регистрации
**Когда отправляется:** При регистрации нового пользователя  
**Цель:** Подтвердить email-адрес после создания аккаунта

### 2. `reset-password.html` — Сброс пароля
**Когда отправляется:** При запросе восстановления пароля  
**Цель:** Предоставить безопасную ссылку для создания нового пароля

### 3. `change-email.html` — Изменение email
**Когда отправляется:** При изменении email-адреса в настройках профиля  
**Цель:** Подтвердить новый email-адрес

### 4. `magic-link.html` — Вход по ссылке (опционально)
**Когда отправляется:** При использовании входа без пароля  
**Цель:** Предоставить одноразовую ссылку для быстрого входа

---

## 🎨 Дизайн

Все шаблоны оформлены в едином стиле вашего приложения:

- **Основной акцент:** Indigo (#6366F1)
- **Градиенты:** Cyan → Blue → Indigo
- **Шрифт:** Inter (с fallback на системные шрифты)
- **Радиус скругления:** 10px
- **Адаптивность:** Корректно отображаются на всех устройствах и email-клиентах

---

## 🚀 Как использовать шаблоны

### Шаг 1: Откройте Supabase Dashboard

1. Перейдите в ваш проект Supabase
2. Откройте: **Authentication** → **Email Templates**

### Шаг 2: Выберите тип шаблона

Вы увидите 4 категории писем:
- **Confirm signup** (Подтверждение регистрации)
- **Reset password** (Сброс пароля)
- **Change email address** (Изменение email)
- **Magic Link** (Вход по ссылке)

### Шаг 3: Скопируйте HTML-код

1. Откройте нужный файл из этой папки в VS Code
2. Нажмите `Ctrl+A` (выделить всё)
3. Нажмите `Ctrl+C` (скопировать)

### Шаг 4: Вставьте в Supabase

1. Вернитесь в Supabase Dashboard
2. Нажмите на нужный тип шаблона
3. **Удалите** старый код из поля "Message Body"
4. Нажмите `Ctrl+V` (вставить ваш новый HTML)
5. Нажмите **Save**

### Шаг 5: Протестируйте

1. Создайте тестовый аккаунт или запросите сброс пароля
2. Проверьте свой email
3. Убедитесь, что письмо выглядит корректно

---

## ⚙️ Настройка email-верификации

### Для включения подтверждения email при регистрации:

#### 1. Измените код на сервере

Откройте `/supabase/functions/server/index.tsx` и найдите endpoint `/signup`:

**Было:**
```typescript
const { data, error } = await supabase.auth.admin.createUser({
  email,
  password,
  user_metadata: { 
    name: name || email.split('@')[0],
    role: userRole,
  },
  email_confirm: true, // ← УДАЛИТЕ эту строку
});
```

**Стало:**
```typescript
const { data, error } = await supabase.auth.admin.createUser({
  email,
  password,
  user_metadata: { 
    name: name || email.split('@')[0],
    role: userRole,
  },
  // email_confirm: true — убрали эту строку
});
```

#### 2. Настройте SMTP (опционально)

По умолчанию Supabase использует встроенный email-сервис (бесплатно до 30,000 писем/месяц).

Если хотите использовать свой SMTP:

1. Supabase Dashboard → **Project Settings** → **Auth**
2. Прокрутите до раздела **SMTP Settings**
3. Включите **Enable Custom SMTP**
4. Введите настройки вашего SMTP-сервера:
   - **Host:** smtp.example.com
   - **Port:** 587
   - **Username:** your-email@example.com
   - **Password:** your-smtp-password

**Популярные SMTP-провайдеры:**
- SendGrid (бесплатно 100 писем/день)
- Mailgun (бесплатно 5,000 писем/месяц)
- Amazon SES (очень дешево)
- Gmail SMTP (для тестирования)

#### 3. Включите email-верификацию

1. Supabase Dashboard → **Authentication** → **Settings**
2. Найдите **Email Auth**
3. Убедитесь, что включено:
   - ✅ **Enable Email Confirmations**

---

## 🔧 Кастомизация шаблонов

### Доступные переменные Supabase

В HTML-шаблонах вы можете использовать эти переменные:

| Переменная | Описание | Пример |
|------------|----------|--------|
| `{{ .ConfirmationURL }}` | Ссылка для подтверждения | https://your-app.com/confirm?token=... |
| `{{ .Token }}` | Токен подтверждения | abcd1234efgh5678 |
| `{{ .TokenHash }}` | Хэш токена | hash_value |
| `{{ .SiteURL }}` | URL вашего приложения | https://your-app.com |
| `{{ .Email }}` | Email пользователя | user@example.com |
| `{{ .Data.name }}` | Имя из user_metadata | Иван Иванов |
| `{{ formatDate now "2006" }}` | Текущий год | 2025 |

### Как изменить цвета

Все цвета в шаблонах взяты из вашего `theme.css`:

```html
<!-- Основной акцент Indigo -->
<a style="background-color: #6366F1;">Кнопка</a>

<!-- Градиент -->
<div style="background: linear-gradient(135deg, #06b6d4, #6366F1);">
```

Чтобы изменить цвет, просто замените HEX-код на нужный.

### Как добавить логотип

Найдите в шаблоне эту строку:

```html
<h1 style="margin: 0; color: #ffffff; font-size: 28px;">
  Добро пожаловать! 👋
</h1>
```

Замените на:

```html
<img src="https://your-cdn.com/logo.png" 
     alt="Logo" 
     style="max-width: 150px; height: auto;">
```

---

## ✅ Чеклист перед запуском

- [ ] Скопировал все 4 шаблона в Supabase Dashboard
- [ ] Убрал `email_confirm: true` из `/supabase/functions/server/index.tsx`
- [ ] Включил **Enable Email Confirmations** в Supabase Auth Settings
- [ ] Настроил SMTP (если нужен кастомный)
- [ ] Протестировал регистрацию с реальным email
- [ ] Проверил, что письма приходят и выглядят корректно
- [ ] Убедился, что ссылки подтверждения работают

---

## 🐛 Troubleshooting

### Письма не приходят

1. **Проверьте папку "Спам"** — первые письма часто попадают туда
2. **Проверьте SMTP настройки** — если используете кастомный SMTP
3. **Проверьте лимиты** — Supabase Free Tier: 30,000 писем/месяц
4. **Проверьте логи:**
   - Supabase Dashboard → **Logs** → **Edge Functions**

### Письма приходят, но ссылка не работает

1. **Проверьте redirectTo URL** в настройках Auth:
   - Supabase Dashboard → **Authentication** → **URL Configuration**
   - Добавьте ваш домен в **Redirect URLs**

2. **Проверьте, что сайт доступен:**
   - `{{ .SiteURL }}` должен указывать на работающий домен

### Дизайн письма сломан

1. **Email-клиенты капризны** — используйте только inline-стили
2. **Проверьте HTML-валидность** — не должно быть незакрытых тегов
3. **Тестируйте в разных клиентах:**
   - Gmail (веб и приложение)
   - Outlook
   - Apple Mail
   - Яндекс.Почта

---

## 📚 Полезные ссылки

- [Документация Supabase Auth](https://supabase.com/docs/guides/auth)
- [Email Templates в Supabase](https://supabase.com/docs/guides/auth/auth-email-templates)
- [SMTP настройки](https://supabase.com/docs/guides/auth/auth-smtp)
- [Примеры email-шаблонов](https://github.com/supabase/auth/tree/master/internal/api/templates)

---

## 💡 Советы

1. **Храните шаблоны в Git** — это резервная копия и история изменений
2. **Тестируйте перед запуском** — отправьте себе тестовое письмо
3. **Делайте шаблоны адаптивными** — используйте `max-width` для мобильных
4. **Следите за размером** — слишком тяжелые письма могут не загрузиться
5. **Используйте безопасные шрифты** — не все шрифты работают в email

---

Если возникнут вопросы — обращайтесь! 🚀
